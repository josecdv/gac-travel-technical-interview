{% extends 'base.html.twig' %}

{% block title %}
	Incrementar stock - Stockify
{% endblock %}

{% block body %}
	<div class="wrapper">
		{% include('menu.html.twig') with {'activeSection': 'categories'} %}

		<div class="content-page">
			<div class="container-fluid">
				<div class="row">
					<div class="col-lg-12">
						<div class="d-flex flex-wrap flex-wrap align-items-center justify-content-between mb-4">
							<div>
								<h4 class="mb-3">Añadir stock</h4>
							</div>
						</div>
					</div>
					<div class="col-lg-12">
						{# {{ include('products/_form.html.twig', {'button_label': 'Update'}) }} #}
						{{ form(form) }}

					
					</div>
				</div>
			</div>
		</div>
	</div>

{% endblock body %}


