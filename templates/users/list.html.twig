{% extends 'base.html.twig' %}

{% block title %}Usuarios Stockify{% endblock %}

{% block body %}
	{% include('menu.html.twig') with {'activeSection': 'users'} %}
	<div class="wrapper">
		<div class="content-page">
			<div class="container-fluid">
				<div class="row">
					<div class="col-lg-12">
						<div class="d-flex flex-wrap flex-wrap align-items-center justify-content-between mb-4">
							<div>
								<h4 class="mb-3">Usuarios</h4>
							</div>
							<a href={{ path('app_users_new') }} class="btn btn-primary add-list">
								<i class="las la-plus mr-3"></i>Nuevo usuario
							</a>
						</div>
					</div>
					<div class="col-lg-12">
						<div class="table-responsive mb-3">
							<table class="table mb-0 tbl-server-info no-footer" role="grid">
								<thead class="bg-white text-uppercase">
									<tr class="ligth ligth-data" role="row">
										<th class="text-left">Nombre</th>
										<th class="text-center">Código</th>
										<th class="text-right">Acciones</th>
									</tr>
								</thead>
								<tbody>
									{% for user in users %}
										<tr>
											<td class="text-left">{{user.username}}</td>
											<td class="text-center">{{user.id}}</td>
											{# <td> <a href="{{ path('app_users_show', {'id': user.id}) }}">show</a> </td> #}
											<td class="text-right">
												<a class="badge bg-warning" data-toggle="tooltip" data-placement="top" title="" data-original-title="Editar" href="{{ path('app_users_edit', {'id': user.id}) }}">
													<i class="ri-pencil-line"></i>
												</a>
											</td>
										</tr>
									{% endfor %}
								</tbody>
							</table>
						</div>
					</div>
				</div>
			</div>
		</div>
	</div>

{% endblock %}

